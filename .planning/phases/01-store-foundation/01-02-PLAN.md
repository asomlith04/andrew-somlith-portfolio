---
phase: 01-store-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - "[DNS / Shopify Admin] Domain DNS records"
  - "[Shopify Admin] Settings > Domains"
autonomous: false
requirements:
  - FOUND-02
  - FOUND-04
user_setup:
  - service: domain-registrar
    why: "Domain purchase and DNS configuration requires human action in external registrar or Shopify admin billing"
    env_vars: []
    dashboard_config:
      - task: "Check faunaluxe.com availability and purchase domain"
        location: "Shopify Admin > Settings > Domains > Buy new domain OR your preferred registrar"
      - task: "If buying through Shopify: domain auto-connects to store (DNS and SSL handled automatically)"
        location: "Shopify Admin > Settings > Domains > Buy new domain"
      - task: "If buying externally: set A record to 23.227.38.65 and CNAME www to shops.myshopify.com at registrar"
        location: "Your registrar's DNS management panel"

must_haves:
  truths:
    - "The custom domain resolves to the Shopify store (not a parked page or error)"
    - "The store loads over HTTPS with no certificate warnings in Chrome and Safari"
    - "Both the root domain (faunaluxe.com) and www subdomain redirect to the store correctly"
  artifacts:
    - path: "[Shopify Admin] Settings > Domains"
      provides: "Domain connected status with SSL"
      contains: "Domain listed as primary with green SSL checkmark"
    - path: "[DNS] A record"
      provides: "Root domain routing to Shopify"
      contains: "@ → 23.227.38.65"
    - path: "[DNS] CNAME record"
      provides: "www subdomain routing to Shopify"
      contains: "www → shops.myshopify.com"
  key_links:
    - from: "Domain A record (23.227.38.65)"
      to: "Shopify SSL provisioning"
      via: "Shopify auto-provisions TLS cert after valid DNS is detected"
      pattern: "Shopify Admin > Settings > Domains shows green SSL status"
    - from: "SSL certificate active"
      to: "HTTPS loads without warning"
      via: "Browser validates cert against domain"
      pattern: "Browser address bar shows padlock with no warning on custom domain"
---

<objective>
Purchase the FAUNA LUXE custom domain, connect it to the Shopify store, and verify SSL is active so the store loads at faunaluxe.com (or chosen alternative) over HTTPS with no certificate warnings.

Purpose: FOUND-02 and FOUND-04 are coupled — a connected domain is required before Shopify can provision an SSL certificate. Neither requirement can be verified without the other.
Output: Custom domain live at Shopify store with green SSL, HTTPS loading confirmed in both Chrome and Safari.
</objective>

<execution_context>
@C:/Users/andre/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/andre/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-store-foundation/01-RESEARCH.md
@.planning/phases/01-store-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: Purchase custom domain and connect to Shopify store</name>
  <files>N/A — external action at domain registrar and Shopify Admin > Settings > Domains</files>
  <action>
    Human-only action: Purchase the FAUNA LUXE domain and connect it to the Shopify store. Domain purchase requires billing that Claude cannot perform.

    STEP 1 — Check faunaluxe.com availability:
    - Shopify Admin > Settings > Domains > Buy new domain → search "faunaluxe.com"
    - If taken, evaluate in order: fauna-luxe.com → faunaluxe.shop → faunaluxe.co → faunaluxe.store
    - Decide before purchasing — do not buy a placeholder

    SHOPIFY DOMAIN (recommended, ~$14/yr):
    - Buy through Shopify Admin > Settings > Domains > Buy new domain
    - DNS auto-connects; SSL provisions automatically — no further action needed

    EXTERNAL REGISTRAR (if preferred, ~$8-12/yr at Namecheap):
    - At registrar DNS: A record @ → 23.227.38.65, CNAME www → shops.myshopify.com
    - Disable DNSSEC at registrar (blocks Shopify SSL if enabled)
    - Cloudflare users: grey cloud DNS-only mode (not orange proxy)
    - Shopify Admin > Settings > Domains > Connect existing domain → enter domain

    After domain is connected: set as Primary domain in Settings > Domains.
  </action>
  <verify>
    <automated>MISSING — domain purchase requires billing; verify manually in Shopify Admin > Settings > Domains</automated>
    <manual>Domain appears in Shopify Admin > Settings > Domains as primary domain; SSL status shows "pending" or green checkmark</manual>
  </verify>
  <done>Custom FAUNA LUXE domain purchased and connected to Shopify as primary domain</done>
  <what-built>N/A — domain purchase requires billing action and registrar interaction Claude cannot perform</what-built>
  <how-to-verify>
    STEP 1 — Check domain availability:
    1. In Shopify Admin: Settings > Domains > Buy new domain
    2. Search for "faunaluxe.com"
    3. If available: purchase through Shopify (RECOMMENDED — DNS auto-connects, SSL provisions automatically, ~$14/yr)
    4. If faunaluxe.com is taken, evaluate in order: fauna-luxe.com → faunaluxe.shop → faunaluxe.co → faunaluxe.store
       — Decide on the domain before proceeding. Do not buy a placeholder.

    SHOPIFY DOMAIN PATH (recommended):
    - Buy domain directly in Shopify Admin > Settings > Domains > Buy new domain
    - After purchase: domain appears in Domains list, auto-connected, DNS configured by Shopify
    - No further DNS action required — proceed to Task 2 to wait for SSL

    EXTERNAL REGISTRAR PATH (if you prefer Namecheap, etc.):
    - Purchase domain at registrar (~$8-12/yr for .com at Namecheap)
    - At registrar DNS settings, set EXACTLY:
        Type: A       Host: @    Value: 23.227.38.65    TTL: Auto
        Type: CNAME   Host: www  Value: shops.myshopify.com  TTL: Auto
    - IMPORTANT: If using Cloudflare nameservers, set to DNS-only mode (grey cloud icon, not orange proxy)
    - IMPORTANT: Disable DNSSEC at the registrar if enabled — it blocks Shopify's SSL provisioning
    - In Shopify Admin: Settings > Domains > Connect existing domain > enter your domain
    - Shopify will verify DNS and begin SSL provisioning

    AFTER DOMAIN IS CONNECTED:
    - Set the new domain as "Primary domain" in Shopify Admin > Settings > Domains
  </how-to-verify>
  <resume-signal>Type "domain connected" with the exact domain purchased (e.g., "domain connected: faunaluxe.com") when the domain appears in Shopify Admin > Settings > Domains and is set as primary</resume-signal>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: Verify SSL certificate is provisioned and HTTPS loads cleanly</name>
  <files>N/A — SSL is provisioned automatically by Shopify after valid DNS; no files modified</files>
  <action>
    Monitor SSL provisioning and confirm HTTPS loads cleanly on both Chrome and Safari. SSL is automatic — no configuration needed, only verification.

    Timeline: Shopify domain purchase → SSL provisions in 15–60 min. External domain → SSL provisions in 1–48 hours after DNS propagates.

    If SSL is still pending after 4 hours (external domain):
    - Verify DNS at dnschecker.org: A record must resolve to 23.227.38.65 globally
    - Check DNSSEC: must be disabled at registrar
    - Check Cloudflare: must be grey cloud (DNS-only), not orange (proxied)
    - Check for duplicate A records: only ONE A record for @
    - After 24 hours still pending: contact Shopify Support

    Verify both:
    - https://[domain].com → loads store with padlock, no warning
    - https://www.[domain].com → redirects correctly, no warning
  </action>
  <verify>
    <automated>MISSING — SSL provisioning is passive; verify manually by visiting the domain in Chrome and Safari</automated>
    <manual>
      1. Shopify Admin > Settings > Domains: domain shows green SSL checkmark
      2. Chrome: https://[domain].com — padlock visible, no certificate warning
      3. Safari on Apple device: https://[domain].com — padlock visible, no warning
      4. https://www.[domain].com — redirects correctly, no warning
    </manual>
  </verify>
  <done>Custom domain loads over HTTPS with valid SSL certificate in Chrome and Safari; no certificate warnings; both root and www work correctly</done>
  <what-built>Claude will monitor SSL provisioning status and guide troubleshooting if SSL is pending beyond normal timeframe. Domain was connected in Task 1.</what-built>
  <how-to-verify>
    SSL PROVISIONING TIMELINE:
    - Shopify domain purchase: SSL typically provisions within 15–60 minutes
    - External domain with correct DNS: SSL provisions within 1–48 hours after DNS propagates

    CHECK SSL STATUS:
    1. Shopify Admin > Settings > Domains
    2. Your domain should show a green checkmark next to "SSL certificate" — if it shows "pending", wait and check again in 30 minutes
    3. Once SSL shows green: open a new browser tab (Chrome) and navigate to https://[yourdomain.com]
    4. Verify: padlock icon appears, no "Not Secure" warning, no certificate error page
    5. Open Safari on any Apple device and navigate to https://[yourdomain.com] — same verification

    IF SSL IS STILL PENDING AFTER 4 HOURS:
    Diagnose in order:
    - External registrar only: Verify DNS at dnschecker.org — A record must resolve to 23.227.38.65 globally
    - Check for DNSSEC: registrar DNS settings — if enabled, disable it and wait 30 minutes
    - Check for Cloudflare proxy: if using Cloudflare, confirm the domain record shows grey cloud (DNS only), not orange (proxied)
    - Check for duplicate A records: registrar DNS must have exactly ONE A record for @
    - If still pending after 24 hours: contact Shopify Support with "SSL certificate pending" — they can manually trigger provisioning

    VERIFY BOTH ROOT AND WWW:
    - https://faunaluxe.com → loads store (no warning)
    - https://www.faunaluxe.com → redirects to root domain (no warning)
  </how-to-verify>
  <resume-signal>Type "https verified" when the custom domain loads over HTTPS with a valid padlock in both Chrome and Safari with no certificate warnings</resume-signal>
</task>

</tasks>

<verification>
FOUND-02 and FOUND-04 are complete when:
- Shopify Admin > Settings > Domains shows the custom domain as primary with green SSL checkmark
- https://[domain].com loads in Chrome with padlock, no certificate warning
- https://[domain].com loads in Safari with padlock, no certificate warning
- https://www.[domain].com redirects correctly (no warning)
- The store, not a parked domain or error page, is what loads at the domain
</verification>

<success_criteria>
- Custom domain purchased and confirmed (not a placeholder — the actual FAUNA LUXE domain)
- Domain connected to Shopify as primary domain
- SSL certificate active (green in Shopify admin)
- Store loads at custom domain over HTTPS with valid certificate in Chrome and Safari
- Both root domain and www subdomain work correctly
</success_criteria>

<output>
After completion, create `.planning/phases/01-store-foundation/01-02-SUMMARY.md` with:
- Exact domain purchased (e.g., faunaluxe.com)
- Registrar used (Shopify or external — and which one)
- DNS records configured (if external registrar)
- SSL provisioning time (how long it took)
- HTTPS verification confirmation (both browsers)
- Any troubleshooting steps taken
</output>
